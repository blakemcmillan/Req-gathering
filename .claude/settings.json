{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "filter": "jq -r 'select(.tool_input.file_path | test(\"/output/.*prd\\.md$\")) | .tool_input.file_path'",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty') && [[ ! -z \"$FILE\" ]] && EVAL_FILE=\"${FILE%.md}-eval.html\" && echo && echo \"📊 PRD EVALUATION RESULTS:\" && echo \"════════════════════════════════\" && python3 .claude/skills/prd-creation/eval-prd.py \"$FILE\" \"$EVAL_FILE\" 2>&1 && echo \"════════════════════════════════\" && echo \"✓ Evaluation complete: $EVAL_FILE\" && echo",
            "statusMessage": "Running PRD quality evaluation..."
          }
        ]
      }
    ]
  }
}
