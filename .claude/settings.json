{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "filter": "jq -r 'select(.tool_input.file_path | endswith(\"GUESS.txt\")) | .tool_input.file_path'",
            "command": "bash guess_auto.sh"
          },
          {
            "type": "command",
            "filter": "jq -r 'select(.tool_input.file_path | test(\"/outputs/.*\\.md$\")) | .tool_input.file_path'",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty') && [[ \"$FILE\" == */outputs/expand-req/*.md ]] && EVAL_FILE=\"${FILE%.md}-eval.html\" && echo && echo \"📊 PRD EVALUATION RESULTS:\" && echo \"════════════════════════════════\" && python3 .claude/skills/expand-req/eval-prd.py \"$FILE\" \"$EVAL_FILE\" 2>&1 && echo \"════════════════════════════════\" && echo \"✓ Evaluation complete: $EVAL_FILE\" && echo",
            "statusMessage": "Running PRD quality evaluation..."
          }
        ]
      }
    ]
  }
}
